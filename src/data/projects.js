// TODO Add a couple lines about each project
const data = Array.from({ length: 30 }, (_, index) => ({
  // title: '',
  // subtitle: '',
  // link: '',
  image: `/images/projects/drawing/projectImage${index + 1}.jpg`,
  // date: '',
  // desc: '',
}));

export default data;
